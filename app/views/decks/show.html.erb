<%  # @cards.group(:colors).count %>

<div class="ui grid">
  <div class="eight wide column">
    <canvas id="card-types-chart"></canvas>
  </div>
  <div class="four wide column">
    <canvas id="mana-costs-chart"></canvas>
  </div>
  <div class="four wide column">
    <% @cards.sample(7).pluck(:name).each do |name| %>
      <%= name %>
      <br />
    <% end %>
  </div>
</div>

