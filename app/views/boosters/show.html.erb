<%= form_for @booster, url: export_booster_path(@booster), method: 'post' do |f| %>
  <div style='float: right; padding-top: 15px'>
    <%= submit_tag 'Exportar para TXT' %>
  </div>
  <%= render 'common/card', cards: @booster.cards %>
<% end %>
